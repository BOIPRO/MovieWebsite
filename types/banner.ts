export interface BannerType {
    _id : string,
    anilistData :{
        seasonYear :number,
        averageScore : number,
        bannerImage : string
    }
    anilistId : number,
    title : string
}